[project]
name = "supersonic-backend"
version = "0.1.0"
description = "Supersonic 智能座舱多屏协同 HMI 原型后端"
requires-python = ">=3.11,<3.13"
dependencies = [
  "alembic>=1.18.5,<1.19",
  "fastapi>=0.116.0,<1.0",
  "pydantic>=2.11.0,<3.0",
  "psycopg[binary]>=3.3.4,<3.4",
  "python-dotenv>=1.1.0,<2.0",
  "sqlalchemy>=2.0.51,<2.1",
  "uvicorn[standard]>=0.35.0,<1.0",
  "pwdlib[argon2]>=0.3,<1.0",
]

[project.optional-dependencies]
vision = [
  "opencv-python>=4.11.0,<5.0",
  "mediapipe>=0.10.21,<1.0",
  "ultralytics>=8.3.0,<9.0"
]
llm = ["openai>=1.93.0,<2.0"]

[dependency-groups]
dev = [
  "httpx>=0.28.0,<1.0",
  "pytest>=8.4.0,<9.0",
  "pytest-asyncio>=1.0.0,<2.0",
  "ruff>=0.12.0,<1.0"
]

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
