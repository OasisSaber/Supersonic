# 实施路线与验收依赖

- 状态：当前可执行工作队列
- 最后更新：2026-08-01
- 项目定位：本地、单用户、可信环境的毕业设计多屏 HMI 原型
- 当前实际状态与验证证据见 [`PROJECT_PROGRESS.md`](./PROJECT_PROGRESS.md)
- 范围与非目标见 [`DECISION_BASELINE.md`](./DECISION_BASELINE.md)

## 1. 执行原则

- 只有“不完成就无法保证基本正确性”的事项才是硬依赖。
- 优先解决状态错误、错误恢复和核心端点缺失，不先建设通用平台。
- 每个可独立实现、关联 PR 和关闭的问题必须有独立 Issue。
- Tracker/路线文档只组织优先级，不长期保存未编号 Bug。
- 规划能力只有具备运行实现、测试或可重复步骤以及展示证据后，才能标记为完成。
- 本地 Mock 闭环可以先于真实地图、数据库、额外 Vision 场景或公开发行能力完成。

## 阶段性目标与退出门

### 阶段 A — 合并门禁恢复

目标是让当前开放 PR 重新获得可信 CI，而不是继续增加并行实现：

- 修复 PR #23–#27 的正文 Markdown 结构并重新触发固定名 `check`；
- 先由人类决定 P0 PR #23，再处理与其文件重叠的 #26；
- PR #25 与 #27 的后合并者必须基于新 `main` restack，重新阅读独立 diff 并完整验证；
- PR #24 记录 GP22 设计基线和已核验来源；即使合并，也不把尚未迁移的 GP22 写成当前代码事实；
- 退出门：目标 PR 的正文校验、仓库验证和项目检查全部由 CI 证明通过，且合并顺序由人类确认。

### 阶段 B — 核心正确性冻结

完成 Issue #11、#7、#12、#17 与 #19，使 session/reset、Overview、风险仲裁、Control
和损坏消息恢复达到当前核心验收。退出门是这些行为已进入 `main`，具有聚焦测试，并能重复演示三条
本地产品流程。

### 阶段 C — 集成证据冻结

完成 Issue #18、#13 与 #14，重点是导航健康一致性、服务端拥有的端点上下文，以及真实 FastAPI
进程下四客户端 snapshot/command/WebSocket 收敛。退出门是核心 `gp05.v1` Smoke 在 CI 通过，且
reset/reconnect 与拒绝行为具有可重复证据。

### 阶段 D — 主要创新

核心 HMI 稳定后，只选择一个真实疲劳/分心 VehicleVision 场景完成输入、候选事件、策略、四屏响应
和降级闭环。退出门是正例、负例、边界用例和失败降级均可重复，不把模拟事件表述成真实推理。

### 阶段 E — 条件性交付增强

仅根据指导教师、学校格式、答辩设备或明确部署要求，从真实地图、持久化、Web3D、第二 Vision
场景、AI 语音或桌面封装中选择少量增强；没有外部触发条件时不进入核心依赖链。

## 2. 已完成基础工作

- `gp05.v1` 合同、端点权限矩阵和权威快照结构；
- FastAPI 内存权威状态、HTTP 命令和 WebSocket 广播；
- Cluster、HUD、Center、Passenger 与 Overview 页面骨架；
- 本地路线、模拟风险接管和副驾协作流程；
- Issue #9 / PR #15：GP21 Token 加载和权威主题绑定；
- Issue #10 / PR #16：仓库根目录统一环境配置；
- GitHub Actions、PR 模板、Markdown/YAML/Shell 检查、Lint、测试和构建入口。

这些完成项不表示整个产品完成。开放缺陷和未实现能力见项目进度文档。

## 3. P0 — 数据与状态完整性

### Issue #11 — Session 排序与 Reset 连通性

这是当前唯一 P0：旧 session 不能长期覆盖新 session，reset 后权威连接状态不能与真实连接相矛盾。

完成 #11 后才能最终冻结 Issue #14 中 reset/reconnect 的真实进程验收预期。

## 4. P1 — 核心产品正确性与答辩功能

### Issue #7 — Overview 严格只读

- 只修命令边界；
- 不进行视觉重设计；
- 不再依赖新的 Figma 画面或三档视觉验收才能开始。

### Issue #12 — 风险仲裁一致性

- 统一 Cluster、HUD、Center 和 Passenger 的主要风险选择；
- 媒体抑制必须与后端“任何 active/acknowledged critical 风险”规则一致。

### Issue #17 — 独立 Control 端点

- `/control` 不再回退为 Center；
- 提供主题、系统模式、模拟场景和 reset 的最小答辩控制；
- 更复杂诊断、历史时间线和外部服务指标不是本 Issue 的前置。

### Issue #19 — WebSocket 与运行时合同容错

- 非法 JSON 或不完整快照不能进入 Store；
- 连接不能在数据已损坏时继续显示为健康；
- 支持无需刷新页面的安全恢复。

## 5. P2 — 近期可靠性、内部权限与验证

### Issue #18 — 导航 Data Health 一致性

本地降级路线、Provider 状态和导航健康值必须使用一致转换规则。该 Issue 不实现真实高德地图。

### Issue #13 — 本地端点权限边界

- 服务端拥有的请求/连接上下文决定端点权限；
- 客户端不能仅修改 JSON 字段升级为其他端点；
- Control 默认禁用或只在显式本地配置下启用；
- 不建设 OAuth、SSO、JWT 平台、多租户或生产车辆 PKI。

### Issue #14 — `gp05.v1` 真实进程 Smoke

硬依赖仅有 Issue #11 的 reset/reconnect 行为。

Issue #12 不阻塞传输收敛测试，Issue #13 也不阻塞当前允许命令和当前拒绝行为的 Smoke。#13 完成后可扩展负向权限场景，但不应让核心多客户端测试长期等待。

## 6. 核心稳定后的主要创新

### 一个真实 VehicleVision 场景

核心 HMI 稳定后，优先实现一个可重复验证的驾驶员疲劳/分心场景：

- 摄像头或授权视频输入；
- 明确的正例、负例和边界用例；
- 候选事件进入风险/策略层；
- 四屏响应 `active → acknowledged → resolved`；
- 无摄像头、缺模型和低置信度时有可见降级；
- 不把手动注入或预设动画描述成真实推理。

该场景是推荐的毕业设计主要技术差异化方向，但不应在 P0/P1 缺陷修复前扩大为通用 Vision 平台。

## 7. 推荐增强

以下工作有价值，但默认不阻塞核心答辩闭环：

- 三档分辨率视觉回归和截图证据；
- 简单事件历史或行程报告持久化；
- 第二个 Vision 场景；
- 目标机性能报告和降级演练；
- Web3D 状态展示及静态 fallback；
- 真实地图 Provider。

这些增强应分别建立有边界的 Issue，不要合并成一个长期无法关闭的“全链路平台”任务。

## 8. 条件性工作

只有触发条件成立时才实施：

| 工作 | 触发条件 |
| --- | --- |
| MySQL、Alembic 和完整审计链 | 指导教师、论文论证或历史查询明确需要数据库 |
| 高德真实地点检索 | 答辩必须展示真实地点搜索，本地路线不能满足要求 |
| 第三类及更多 Vision 场景 | 第一个真实场景已稳定，且答辩收益高于剩余成本 |
| AI 语音 | 核心 HMI、恢复、Smoke 和主要创新证据全部稳定 |
| Electron/Tauri 封装 | 浏览器多屏启动不能满足现场部署需求 |
| Windows 签名或 macOS 公证 | 准备向外部分发正式安装包 |
| 正式认证、多租户、高可用 | 服务进入不可信网络、公共云或多人共享部署 |
| 容器化 | 出现可复现部署需求且脚本安装不能满足 |

条件未成立时，这些工作不进入核心依赖链。

## 9. 当前不计划事项

当前项目不建设：

- 企业 SSO、多租户账户体系；
- 公共互联网 API 网关、WAF 或通用 SSRF 平台；
- 量产车 PKI、CAN 安全网关或功能安全认证；
- ROS2、CARLA、MQTT 或通用消息中间件；
- 多节点高可用和分布式一致性；
- 全平台安装包和完整软件供应链证明；
- 数字人、情绪识别、XR 或复杂 LLM 自主驾驶决策。

若未来定位发生变化，应通过新的决策 Issue 重新评估，而不是在当前 Backlog 中提前建设。

## 10. 推荐实施顺序

1. Issue #11；
2. Issues #7、#12、#17、#19，可按文件冲突情况并行；
3. Issues #18、#13；
4. Issue #14；
5. 一个真实疲劳/分心 VehicleVision 场景；
6. 根据指导教师和答辩需求，从持久化、真实地图、第二 Vision 场景、Web3D 中选择少量增强；
7. AI 语音和发行工作继续保持条件性。

## 11. 当前核心验收标准

进入答辩功能冻结前，应至少证明：

- 四个产品端点和 Control 使用同一权威 session/revision；
- Overview 不可发送业务命令；
- 导航接力、模拟风险处置和副驾协作可确定性重演；
- 新 session、reset、断线和非法消息可以安全恢复；
- 前后端对主要风险和媒体抑制没有矛盾；
- 真实进程四客户端 Smoke 在 CI 通过；
- Mock、真实、降级和未实现能力被明确区分；
- README、进度、路线和代码事实一致。

真实地图、MySQL、Web3D、多个 Vision 场景和 AI 语音不属于默认核心验收的硬依赖。
